name: CI

on:
  push:
    branches: [main]
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
  workflow_call:
    inputs:
      go-version-file:
        description: Path to go.mod for version detection
        required: false
        type: string
        default: go.mod

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  NODE_TLS_REJECT_UNAUTHORIZED: 0

jobs:
  lint:
    name: Lint
    if: github.event_name == 'workflow_call' || github.event.pull_request.draft == false
    runs-on: ubuntu-latest
    steps:
      - name: Set Go environment for act
        if: env.ACT == 'true'
        run: |
          echo "GOPROXY=direct" >> $GITHUB_ENV
          echo "GOSUMDB=off" >> $GITHUB_ENV
          echo "GOINSECURE=proxy.golang.org,github.com,cel.dev" >> $GITHUB_ENV

      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: ${{ inputs.go-version-file || 'go.mod' }}
          cache: true

      - name: Run golangci-lint
        if: env.ACT != 'true' # Skip in act due to TLS schema download issues
        uses: golangci/golangci-lint-action@v9
        with:
          version: latest
          args: --skip-config-verify

  test:
    name: Test
    if: github.event_name == 'workflow_call' || github.event.pull_request.draft == false
    runs-on: ubuntu-latest
    steps:
      - name: Set Go environment for act
        if: env.ACT == 'true'
        run: |
          echo "GOPROXY=direct" >> $GITHUB_ENV
          echo "GOSUMDB=off" >> $GITHUB_ENV
          echo "GOINSECURE=proxy.golang.org,github.com,cel.dev" >> $GITHUB_ENV

      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: ${{ inputs.go-version-file || 'go.mod' }}
          cache: true

      - name: Verify dependencies
        if: env.ACT != 'true' # Skip in act due to TLS proxy issues
        run: go mod verify

      - name: Check for untidy modules
        if: env.ACT != 'true'
        run: |
          go mod tidy
          git diff --exit-code go.mod go.sum

      - name: Run all tests
        if: env.ACT != 'true'
        run: go test -v -race -timeout 15m -coverpkg=./internal/... -coverprofile=coverage.out -covermode=atomic ./...
        env:
          APP_ENV: test

      - name: Verify Swagger docs
        if: env.ACT != 'true'
        run: |
          go install github.com/swaggo/swag/cmd/swag@latest
          swag init -g main.go -o docs --parseDependency --parseInternal
          git diff --exit-code docs/

      - name: Upload coverage report
        if: env.ACT != 'true'
        uses: actions/upload-artifact@v4
        with:
          name: coverage
          path: coverage.out

  build:
    name: Build Check
    if: github.event_name == 'workflow_call' || github.event.pull_request.draft == false
    runs-on: ubuntu-latest
    steps:
      - name: Set Go environment for act
        if: env.ACT == 'true'
        run: |
          echo "GOPROXY=direct" >> $GITHUB_ENV
          echo "GOSUMDB=off" >> $GITHUB_ENV
          echo "GOINSECURE=proxy.golang.org,github.com,cel.dev" >> $GITHUB_ENV

      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: ${{ inputs.go-version-file || 'go.mod' }}
          cache: true

      - name: Build binary
        if: env.ACT != 'true'
        run: go build -v -o /dev/null ./main.go
